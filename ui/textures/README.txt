Figma-exported UI bitmaps (9-slice panels, button states, icons) live here unless they are shared gameplay art (then use assets/).

Import defaults: Filter off for pixel UI if matching 64px-style art; enable Filter for smooth panels.
